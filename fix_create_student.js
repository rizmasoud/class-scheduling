const fs = require('fs');
let code = fs.readFileSync('src/features/students/components/CreateStudentDialog.tsx', 'utf-8');

// I will use multi_edit or just rewrite it using the script?
// Let's use string replacement or write a full typescript file.
